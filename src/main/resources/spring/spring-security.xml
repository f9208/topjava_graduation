<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">


    <http pattern="/rest/**" use-expressions="true" name="restSecurityFilterChain">
        <http-basic/>
        <!--        //todo пересмотреть права доступа, пока что оставил так.-->
        <intercept-url pattern="/rest/admin/**" access="hasRole('ADMIN')"/>
        <intercept-url pattern="/rest/restaurants/**" access="permitAll()" method="GET"/>
        <intercept-url pattern="/rest/register" access="isAnonymous()" method="POST"/>
        <csrf disabled="true"/>
    </http>


    <http pattern="/**" use-expressions="true" name="uiSecurityFilterChain">
        <http-basic/>
        <intercept-url pattern="/admin/**" access="hasRole('ADMIN')"/>
<!--        <intercept-url pattern="/restaurants/**" access="isAuthenticated()" method="GET"/>-->
        <intercept-url pattern="/restaurants/**/vote" access="isAuthenticated()" method="POST"/>
        <intercept-url pattern="/restaurants/**/edit" access="hasRole('ADMIN')"/>
        <intercept-url pattern="/restaurants/**/add" access="hasRole('ADMIN')"/>
        <intercept-url pattern="/register" access="isAnonymous()" method="POST"/>
        <intercept-url pattern="/profile/**" access="isAuthenticated()"/>
        <intercept-url pattern="/votes/**" access="isAuthenticated()"/>

        <form-login login-page="/login" default-target-url="/"
                    authentication-failure-url="/?error=true"
                    login-processing-url="/spring_security_check"/>
        <logout logout-success-url="/"/>
        <csrf disabled="true"/>
    </http>


    <beans:bean class="org.springframework.security.crypto.factory.PasswordEncoderFactories" id="passwordEncoder"
                factory-method="createDelegatingPasswordEncoder"/>
    <authentication-manager>
        <authentication-provider user-service-ref="userService">
            <password-encoder ref="passwordEncoder"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>